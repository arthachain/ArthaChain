// Minimal models module to satisfy workspace builds for the root crate
// Declare submodules that exist in this directory
pub mod bci_interface;
pub mod neural_base;
pub mod registry;

// If more modules are added here later, declare them above.

